#include <bits/stdc++.h>
#define TAM 200005

using namespace std;

int main(){
	int cases, n, k;
	int arreglo[TAM];
	cin >> cases;

	while(cases--){
		cin >> n >> k;

		for(int i = 0; i < n; i++)
			cin >> arreglo[i];
		
		
	}


	return 0;
}