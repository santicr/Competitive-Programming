#include <stdlib.h>
#include <stdio.h>
#include <string.h>

int main(){
	int n;
	char* cadena;
	scanf("%d", &n);
	while(n != 0){
		scanf("%s", cadena);
		int i, j;
		for(i = 0; i < strlen(cadena); i += n){
			for(j = 0; j < strlen(cadena))
		}
		scanf("%d", &n);
	}
	return 0;
}