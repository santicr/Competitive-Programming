#include <iostream>
#include <string>
#include <vector>

using namespace std;

int main(){
	int cases, clues, i, j;
	cin >> cases;

	while(cases--){
		cin >> clues;
		int num, indice = 0, correct_pos, wrong_pos;
		vector <int> correct, wrong;
		vector <vector> vec;

		for(i = 0; i < clues; i++){
			scanf("%d %d/%d", &num, &correct_pos, &wrong_pos);
			vec.push_back(number);
		}
		
	}


	return 0;
}